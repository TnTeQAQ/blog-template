# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local env overrides (the default .env and .env.example ARE committed)
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Editor atomic-write / backup leftovers (UUID-name with a numeric suffix)
*.[0-9]*

# --- personal blog content & workers (kept local, never pushed to the template) ---
# Your posts live only in this working copy and are published by `npm run
# deploy`. Only the demo post is shared with the public template.
content/posts/*
!content/posts/hello-world.md
!content/posts/hello-world/
!content/posts/hello-world/**

# Personal domain + local planning notes.
CNAME
public/CNAME
PLAN.md

# Deploy scratch directory (rewritten by scripts/deploy.mjs).
.deploy
